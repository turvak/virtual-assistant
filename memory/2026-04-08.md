- Attempted to access 1Password to check for "N8N Claw Agent API" credentials.
- Encountered a credential decoding error with the `op` CLI (`failed to DecodeSACredentials`, `illegal base64 data`).
- Identified a likely issue with the `OP_SERVICE_ACCOUNT_TOKEN` in the `.env` file or environment.
- Marc confirmed n8n credentials should be in the vault under "N8N Claw Agent API".
- Noted that `attendell-n8n` container is running on the host but direct API/CLI access is not yet configured for Artie.
